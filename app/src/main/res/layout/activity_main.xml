<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="#FCF3CF">

    <TextView
        android:id="@+id/emptyBox1"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/welcome"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:textColor="@color/black"
        android:textSize="40dp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:text="Welcome to ABC Coffee Shop"
        app:layout_constraintTop_toBottomOf="@+id/emptyBox1" />

    <TextView
        android:id="@+id/emptyBox2"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        app:layout_constraintTop_toBottomOf="@+id/welcome" />

    <TextView
        android:id="@+id/hint"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="Please login to generate the qr code for discount"
        android:textColor="@color/black"
        android:textSize="15dp"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@+id/emptyBox2" />

    <Button
        android:id="@+id/loginBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/hint"/>

    <TextView
        android:id="@+id/emptyBox999"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        app:layout_constraintTop_toBottomOf="@+id/loginBtn"
        />

    <TextView
        android:id="@+id/address"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        app:layout_constraintTop_toBottomOf="@+id/emptyBox999"
        android:text="Shop address: 30 Shing Tai Road, Chai Wan, Hong Kong"
        android:textAlignment="center"
        android:textColor="@color/black"
        />

</androidx.constraintlayout.widget.ConstraintLayout>